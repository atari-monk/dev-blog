To install **Rhythmbox** on Ubuntu, open a terminal and run:

```bash
sudo apt update
sudo apt install rhythmbox
```

That’s it 👍
After installation, you can launch it from the Applications menu or by typing:

```bash
rhythmbox
```
