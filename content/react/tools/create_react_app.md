# Create React App

Create React App is older — many developers now use Vite, which is faster and lighter.

```sh
npm create react-app my-app
# or
yarn create react-app my-app
cd my-app
npm start
```
