# *cheat sheet** for PowerShell

- create a new file and put clipboard content into it

```powershell
Get-Clipboard > filename.py
```

This creates (or overwrites) the file with whatever text you currently have copied in your clipboard.
